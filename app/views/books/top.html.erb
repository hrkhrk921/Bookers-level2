<div class="body">
	<div class="col-xs-3">
		<h2>User info</h2>
	</div>
	<div class="col-xs-3">
		<%= form_for(@book) do |f| %>
		<h4>Title</h4>
			<%= f.text_field :text %>
		<h4>Opinion</h4>
			<%= f.text_area :body %>
			<%= f.submit 'Create Book' %>
		<% end %>
	</div>
</div>